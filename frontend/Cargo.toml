[package]
name = "frontend"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "frontend"
path = "src/main.rs"

[dependencies]
leptos = { version = "0.8.19", features = ["csr"] }
shared = { version = "0.1.0", path = "../shared" }
url = { workspace = true }
